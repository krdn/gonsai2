# ==================================================
# Frontend Environment Variables - PRODUCTION
# ==================================================
# IMPORTANT: Copy this file to .env.production and fill in real values
# NEVER commit .env.production with real secrets!
# NOTE: NEXT_PUBLIC_ variables are included in the build
# ==================================================

# API URL - Nginx reverse proxy
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# WebSocket URL - Nginx를 통해 백엔드 WebSocket에 접근
NEXT_PUBLIC_WS_URL=wss://your-domain.com/ws

# Socket.io URL - AI Agents 페이지에서 사용
# 비워두면 window.location.origin을 사용 (더 유연함)
NEXT_PUBLIC_SOCKET_URL=

# n8n Configuration
NEXT_PUBLIC_N8N_BASE_URL=http://localhost:5678
NEXT_PUBLIC_N8N_UI_URL=https://your-n8n-domain.com

# ==================================================
# API Keys
# ==================================================
# WARNING: NEXT_PUBLIC_ keys are exposed to client!
# Consider using server-side API routes instead
# ==================================================
# These should be handled server-side if possible
# NEXT_PUBLIC_N8N_API_KEY=DO_NOT_EXPOSE_TO_CLIENT
# NEXT_PUBLIC_BACKEND_API_KEY=DO_NOT_EXPOSE_TO_CLIENT
